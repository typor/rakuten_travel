<section id="not-found">
  <div class="container">
    <div class="box">
      <div class="center" style="padding-bottom:100px;">
        <h2 class="text-warning small"><%= t('front.not_found') %></h2>
        <p class="lead">
          <%= t('front.not_found_lead') %>
        </p>
        <p>
          <%= link_to icon('home') + raw('&nbsp;&nbsp;') + t('global.back_to_top'), front_dashboard_path, class: 'btn btn-primary btn-lg' %>
        </p>
      </div>
    </div>
  </div>
</section>